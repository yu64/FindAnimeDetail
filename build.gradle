plugins {
    id("quarkus_setting")
}


dependencies {
    
    // REST エンドポイント用 (JAX-RS) 
    implementation 'io.quarkus:quarkus-rest'
    
    // JSONパース・バインディング用 (Jackson)
    implementation 'io.quarkus:quarkus-rest-jackson'
    
    // 外部API取得用 HTTPクライアント
    implementation 'io.quarkus:quarkus-rest-client-jackson'

    // LINE Bot Sdk Model
    implementation("com.linecorp.bot:line-bot-model:6.0.0")
    
    // LINE Bot Sdk Parser
    implementation("com.linecorp.bot:line-bot-parser:6.0.0")

    // mug core (構文パーサー)
    implementation("com.google.mug:mug:10.8.1")

    // mug dot-parse (構文パーサー)
    implementation("com.google.mug:dot-parse:10.8.1")

    testImplementation "org.junit.jupiter:junit-jupiter:5.10.2"
    testImplementation "io.quarkus:quarkus-junit"
    testRuntimeOnly "org.junit.platform:junit-platform-launcher"
    // Quarkusを起動せずにConfigMappingを生成する単体テスト用
    testRuntimeOnly "org.ow2.asm:asm"
}

tasks.test {
    useJUnitPlatform()
}

